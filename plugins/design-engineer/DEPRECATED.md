# Deprecated

`design-engineer` has been folded into the new `product-design` plugin.

This directory is intentionally left unlisted for one migration pass so existing
users can recover prior workflow files if needed. New installs should use:

```text
product-design
```
